# UI 组件能力暴露

这篇文档解决的问题：说明后续如何思考 H5 调用 ArkTS UI 能力，以及为什么当前阶段暂不展开组件能力暴露。

## 当前状态

当前尚未实现，后续阶段补充。

## 初步目标

UI 组件能力暴露会围绕这些问题展开：

- H5 如何通过 action 表达 UI 能力。
- ArkTS 如何控制能力白名单。
- UI 能力如何走 Dispatcher / Registry / Biz / Imp。
- 调试面板如何展示 UI 能力调用链。

## 当前不做的原因

当前阶段先完成 JSBridge 最小闭环。组件能力暴露会等 `ui.showToast` 跑通后再设计。
