# v0.1.0 npm Publish Record

这篇文档解决什么问题：记录 `@lichenyang5/miniapp-runtime-harmony-web-sdk@0.1.0` 首次 npm 发布准备的真实证据和尚未完成事项。

## Current Status

状态：Ready for author review，尚未执行 npm publish。

```text
Package: @lichenyang5/miniapp-runtime-harmony-web-sdk
Version: 0.1.0
Registry: https://registry.npmjs.org/
npm whoami: ENEEDAUTH (not logged in)
Package lookup: E404 (package not currently published)
```

E404 只证明该具体包名当前不存在，不证明当前账号拥有 `@lichenyang5` scope 发布权限。

## Tarball

```text
File: lichenyang5-miniapp-runtime-harmony-web-sdk-0.1.0.tgz
Files: 20
Packed size: about 13.1 kB
Unpacked size: about 60.4 kB
SHA-1: 6c7fad146107f6354fc745d387d91e81e7754f80
```

包含 ESM、IIFE、TypeScript declarations、README、LICENSE、CHANGELOG 和 package.json。未发现 token、私钥、证书、密码、绝对用户路径或 file/workspace/link 依赖。

## Verification

- Root `npm run check`: passed.
- H5 SDK unit tests: 24/24 passed.
- Local tgz consumer: 3/3 passed.
- ESM import and TypeScript typecheck: passed.
- IIFE auto mount: passed.
- Normal browser boundary: `NATIVE_UNAVAILABLE` confirmed.
- New Web consumer build: passed with Vite 6.4.3.
- Independent HarmonyOS HAP build with local HAR: passed.
- Registry install: not run because package is not published.
- ArkWeb device smoke: not run in this preparation step.

## Security Gate

The npm tarball itself is clean. The repository still records that HarmonyOS signing credentials were exposed in public history. The project author must rotate or revoke them and decide how to clean history before treating the broader GitHub release as closed.

## Publish Command

After author identity, scope permission, Git status and security gates are confirmed:

```bash
cd h5_sdk
npm publish --access public
```

Any OTP/2FA step is completed manually by the project author.

## Post-Publish Record

After publish, add the real values here:

```text
Published at:
npm version output:
npm package URL:
Registry consumer install:
HarmonyOS device:
```
