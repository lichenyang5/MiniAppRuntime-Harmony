# v0.1.0 Self-check Report

这篇文档解决什么问题：汇总 v0.1.0 展示候选版的真实证据、未完成项和发布边界，避免把源码存在、自动检查通过和真机验证混为一谈。

首次检查日期：2026-07-15。发布收口复核日期：2026-07-17。

## 自动检查结果

| Check | Status | Evidence |
| --- | --- | --- |
| `npm run check` | 2026-07-17 通过 | generated docs/types、8 个 action、一致性、H5 check 与 package exports 全部通过 |
| `npm --prefix h5_sdk run check` | 2026-07-17 通过 | IIFE/ESM/type declarations 构建成功，13/13 Node tests 通过 |
| `npm pack --dry-run`（`h5_sdk`） | 2026-07-17 通过 | prepack build/test 通过；候选包 18 个文件，约 10.0 kB packed / 41.7 kB unpacked |
| GitHub Actions CI | 基线通过 | GitHub API 确认 `9de3f82` 的 [CI run](https://github.com/lichenyang5/MiniAppRuntime-Harmony/actions/runs/29386035145) 为 success；最终发布 commit 尚未产生 |
| `npm run h5:sync` | 通过 | H5 SDK build 后成功执行 `copy:demo`，IIFE 已同步到 rawfile |
| HarmonyOS HAP build | 本轮本地构建通过，有 warning | 使用 DevEco Studio 自带 Hvigor 构建 `entry@default`，未执行设备启动 |

本轮使用的构建形式为 `node "<DevEco Studio>/tools/hvigor/bin/hvigorw.js" assembleHap --mode module -p module=entry@default`。HAP 构建仍提示 Clipboard 读取权限、部分调用可能抛异常，以及 `showToast`、`getContext` 的废弃警告。这是当前会话的本地构建观察，不代表应用已在设备启动；这些 warning 也没有在本阶段通过业务重构处理。

## 手动功能检查

| Capability | Current evidence | Status |
| --- | --- | --- |
| ArkWeb 本地 H5 | 首页运行截图 | 有证据 |
| Toast | 系统 Toast 运行截图 | 有证据 |
| Clipboard | 首页中可见 Clipboard 区域和成功记录，但缺少独立结果截图 | 待补截图 |
| Storage | setItem resolve 运行截图 | 有证据，完整 set/get/remove/clear 仍按冒烟清单复核 |
| `runtime.getApiList` | API 返回内容截图 | 有证据 |
| DebugPanel | 成功列表与错误记录截图 | 有证据 |
| UNKNOWN_ACTION / PARAM_ERROR | DebugPanel 错误记录包含对应 code | 部分有证据 |
| TIMEOUT | 源码、测试和 Demo 按钮存在 | 待独立设备截图 |
| Web 容器加载状态 | 首页显示 Loading 100%，缺少独立状态截图 | 待补截图 |
| URL Guard / 错误页 | 源码和 Demo 按钮存在 | 待补截图与手工复核 |

截图只能证明画面中可见的场景，不替代完整手工回归记录。

## 文档检查

- README：已核对 Packages、Quick Start、HAR 接入、H5 build/copy、质量命令、pack dry-run、截图引用和 CI badge。
- API docs：由 `tools/api-manifest.json` 生成，并由 generated check 校验。
- H5 SDK docs：包含 IIFE、ESM、typed API 和 npm pack 本地验证边界。
- HAR docs：包含门面接入、架构和新建 Demo 指南。
- Testing docs：区分 Node 自动检查与 DevEco Studio 手工 smoke test。
- Release docs：记录 npm `0.1.0` 已发布、HAR 未发布、未自动 tag 或创建 Release。
- Showcase docs：包含演示路径、技术亮点、讲解稿与自测答案。
- Articles：综合掘金草稿、发布清单与链接回填入口已建立，当前状态为 Draft。
- 本地链接：2026-07-17 脚本检查了 93 个 Markdown 文件，未发现缺失的相对链接或图片引用。

## 当前未完成事项

- 10 类推荐截图尚未全部补齐，目前仓库有 7 张真实图片。
- H5 SDK `@lcy453/miniapp-runtime-harmony-web-sdk@0.1.0` 已发布到 npm。
- HAR 仍通过 `file:../myascf_runtime` 本地依赖接入，未发布到包仓库。
- Network API 未实现，CLI 未实现且当前不计划实现。
- ArkTS Manifest 与工具 JSON 仍是两份需要检查一致性的镜像。
- `build-profile.json5` 中的签名材料配置已经存在于 `origin/main`。相关凭据应按已暴露处理：立即轮换或吊销，从当前文件移除，并评估清理 Git 历史；完成前不应继续发布候选版本。
- `v0.1.0` tag、GitHub Release 和掘金文章均尚未创建或发布。

## 结论

从功能结构、文档和工程化准备看，v0.1.0 已具备展示版候选的主体；从发布安全看，当前结论为“不通过”，首要阻塞项是已经进入远端历史的签名凭据。完成凭据轮换/吊销、当前文件与历史清理后，再结合剩余截图、最新 CI 和完整设备回归重新评估。此前不建议继续加功能或推进发布。
