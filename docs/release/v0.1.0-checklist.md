# v0.1.0 展示版验收清单

这篇文档解决什么问题：把自动检查、设备演示、文档与公开合规要求汇总成一个可勾选的候选版本验收入口。

## 自动检查

- [x] 根目录执行 `npm run check` 成功（2026-07-17）。
- [x] `npm --prefix h5_sdk run check` 成功（2026-07-17）。
- [x] GitHub Actions `CI` 对基线 `9de3f82` 运行成功（[workflow](https://github.com/lichenyang5/MiniAppRuntime-Harmony/actions/runs/29386035145)）。
- [ ] GitHub Actions `CI` 对最终发布 commit 运行成功。
- [x] 在 `h5_sdk` 执行 `npm pack --dry-run` 成功，内容符合包白名单（18 files）。
- [x] 13 个 H5 SDK 单元测试全部通过。
- [x] 10 个 Manifest action（9 个公开、1 个内部）与 ActionNames、ApiManifest、RuntimeBootstrap 对齐。

## 手动检查

- [ ] DevEco Studio 可以同步、构建并启动 `entry` Demo。
- [x] ArkWeb 加载本地 H5 首页（已有真实运行截图）。
- [x] Toast 调用成功（已有系统 Toast 截图）。
- [ ] Clipboard write/read 在目标设备和权限条件下行为符合预期。
- [ ] Storage set/get/remove/clear 调用成功。
- [ ] `runtime.getApiList` 返回当前 9 个公开 API（截图只展示返回内容的一部分，仍需完整手工记录）。
- [ ] DebugPanel 记录成功、错误和 requestId（截图有成功/错误记录，但折叠项未显示 requestId）。
- [ ] TIMEOUT、UNKNOWN_ACTION 与 PARAM_ERROR 可观察且应用不崩溃。
- [ ] Web 容器 loading、success、blocked 和 error/retry 状态可演示。
- [ ] URL Guard 拦截不允许的 URL。
- [ ] 外部 Demo 可通过本地依赖导入 HAR。
- [ ] `examples/sdk-consumer-demo` 可使用候选 tarball 完成 ESM build/test。

完整操作步骤见 [Demo Walkthrough](../showcase/demo-walkthrough.md) 和 [手工冒烟测试](../testing/manual-smoke-test.md)。

## 截图检查

- [ ] 已按 [截图清单](../assets/screenshots/README.md) 采集真实运行图片。
- [ ] 截图不包含账号、隐私路径、通知或非公开信息。
- [x] README 只引用仓库中真实存在的图片。
- [ ] CI 截图来自候选 commit 的真实 Actions 运行。

## 文档检查

- [x] 根 README 的定位、架构、Packages、Quick Start 和 Roadmap 与代码一致。
- [x] `docs/README.md` 可以进入架构、API、HAR、H5 SDK、测试、CI 和 Showcase。
- [x] generated API 文档和 typed API 文档为最新版本。
- [x] HAR usage、H5 SDK usage 和 manual smoke test 已复核。
- [x] Release Notes、Release Plan、GitHub Release 模板和 CHANGELOG 的未发布边界一致。
- [x] 文章草稿、发布清单和 README Articles 状态一致。
- [x] 技术文章导航只标记真实完成状态。

## 合规检查

- [x] 不包含公司内部源码、接口、模块名、文档或业务材料。
- [ ] 不包含客户问题、账号、设备标识或其他隐私信息（CI 截图含公开账号名；`build-profile.json5` 的签名凭据已进入远端历史，需立即轮换/吊销并清理）。
- [x] 不使用与任何非公开框架进行等同或模仿的描述。
- [x] npm 发布状态按 registry 真实记录，HAR 不暗示已发布到包仓库。
- [x] 不使用“生产可用”“完整小程序平台”等未经验证的表述。
- [x] 未自动修改版本、打 tag、创建 GitHub Release 或执行 publish。

## 验收记录

- 候选基线 commit：`9de3f82683fe86b5250d8a801d5bab407471150b`（复核期间 `main`/`origin/main` 已前进到该提交；当前安全修正工作树尚未提交）
- DevEco Studio / SDK：`targetSdkVersion 6.1.0(23)`；本轮使用 DevEco Studio 自带 Hvigor 完成 HAP 构建
- 设备 / 系统版本：`__________`
- 基线 CI 运行链接：`https://github.com/lichenyang5/MiniAppRuntime-Harmony/actions/runs/29386035145`
- 最终发布 commit CI：`__________`
- 手工测试日期：`__________`
- 已知问题：`__________`
- 结论：`不通过`（签名凭据已进入远端历史，必须先轮换/吊销并清理；自动检查和本地 HAP 构建通过不抵消该发布阻塞项）

## 发布动作

- [ ] 已完成签名凭据轮换/吊销、当前文件清理和 Git 历史清理。
- [ ] 已确认最终发布 commit 的 GitHub Actions 成功。
- [ ] 已由项目作者手动创建 `v0.1.0` tag。
- [ ] 已由项目作者手动创建 GitHub Release。
- [ ] 已由项目作者手动发布掘金文章并回填真实链接。
- [x] H5 SDK `0.1.0` 已由项目作者发布到 npm；未执行 HAR/ohpm publish。

操作说明见 [v0.1.0 Publish Record](v0.1.0-publish-record.md)。
