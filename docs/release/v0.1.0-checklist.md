# v0.1.0 展示版验收清单

这篇文档解决什么问题：把自动检查、设备演示、文档与公开合规要求汇总成一个可勾选的候选版本验收入口。

## 自动检查

- [x] 根目录执行 `npm run check` 成功（2026-07-15）。
- [x] `npm --prefix h5_sdk run check` 成功（2026-07-15）。
- [ ] GitHub Actions `CI` workflow 对候选 commit 运行成功。
- [x] `npm --prefix h5_sdk run pack:dry-run` 内容符合包白名单（18 files）。
- [x] 13 个 H5 SDK 单元测试全部通过。
- [x] 8 个 Manifest action 与 ActionNames、ApiManifest、RuntimeBootstrap 对齐。

## 手动检查

- [ ] DevEco Studio 可以同步、构建并启动 `entry` Demo。
- [x] ArkWeb 加载本地 H5 首页（已有真实运行截图）。
- [x] Toast 调用成功（已有系统 Toast 截图）。
- [ ] Clipboard write/read 在目标设备和权限条件下行为符合预期。
- [ ] Storage set/get/remove/clear 调用成功。
- [x] `runtime.getApiList` 返回当前 8 个 API（已有返回内容截图）。
- [x] DebugPanel 记录成功、错误和 requestId（已有成功/错误截图）。
- [ ] TIMEOUT、UNKNOWN_ACTION 与 PARAM_ERROR 可观察且应用不崩溃。
- [ ] Web 容器 loading、success、blocked 和 error/retry 状态可演示。
- [ ] URL Guard 拦截不允许的 URL。
- [ ] 外部 Demo 可通过本地依赖导入 HAR。
- [ ] `examples/sdk-consumer-demo` 可使用候选 tarball 完成 ESM build/test。

完整操作步骤见 [Demo Walkthrough](../showcase/demo-walkthrough.md) 和 [手工冒烟测试](../testing/manual-smoke-test.md)。

## 截图检查

- [ ] 已按 [截图清单](../assets/screenshots/README.md) 采集真实运行图片。
- [ ] 截图不包含账号、隐私路径、通知或非公开信息。
- [x] README 只引用仓库中真实存在的图片。
- [ ] CI 截图来自候选 commit 的真实 Actions 运行。

## 文档检查

- [x] 根 README 的定位、架构、Packages、Quick Start 和 Roadmap 与代码一致。
- [x] `docs/README.md` 可以进入架构、API、HAR、H5 SDK、测试、CI 和 Showcase。
- [x] generated API 文档和 typed API 文档为最新版本。
- [x] HAR usage、H5 SDK usage 和 manual smoke test 已复核。
- [ ] Release Notes、Release Plan、GitHub Release 模板和 CHANGELOG 一致。
- [x] 技术文章导航只标记真实完成状态。

## 合规检查

- [x] 不包含公司内部源码、接口、模块名、文档或业务材料。
- [ ] 不包含客户问题、账号、设备标识或其他隐私信息（截图仍需最终发布前复核，构建配置含本机签名材料）。
- [x] 不使用与任何非公开框架进行等同或模仿的描述。
- [x] 不暗示 H5 SDK 已发布到 npm 或 HAR 已发布到包仓库。
- [x] 不使用“生产可用”“完整小程序平台”等未经验证的表述。
- [x] 未自动修改版本、打 tag、创建 GitHub Release 或执行 publish。

## 验收记录

- 候选 commit：`__________`
- DevEco Studio / SDK：`__________`
- 设备 / 系统版本：`__________`
- CI 运行链接：`__________`
- 手工测试日期：`__________`
- 已知问题：`__________`
- 结论：`有条件通过`（自动检查与 HAP 构建通过；剩余截图、完整设备回归、当前候选 CI 和签名材料清理未完成）
