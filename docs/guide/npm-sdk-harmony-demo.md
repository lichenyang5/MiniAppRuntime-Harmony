# Moved: npm SDK + HAR Demo

这篇文档解决什么问题：保留旧链接并指向当前独立消费 Demo 教程。

教程已迁移到 [npm SDK + HAR Demo](npm-sdk-har-demo.md)。新文档统一使用 `examples/npm-har-consumer-demo`，其 HarmonyOS 工程只消费 `entry/libs/myascf_runtime.har`，不再跨目录引用 Runtime 源码。
